<div>
  <%= flash[:notice] %>
</div>

<div class="container">
  <div class="row">
    <div class="col">
      <%= render 'userinfo', user: @user %>
    </div>
    <div class="col">
      <h1>User info</h1>
<% @books.each do |book| %>
<div>
  <%= image_tag book.user.get_profile_image(70,70) %>
  <%= book.title %>
  <%= book.body %>
</div>
<% end %>

    </div>
  </div>
  <div class="row">
<%= render 'books/new', book: @book %>
</div>
</div>