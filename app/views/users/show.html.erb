<div class="container">
  <div class="row">
    <div class="col-lg-3 mr-30px">
      <div class="row">
        <div class="col">
          <%= render 'userinfo', user: @user %>
        </div>
      </div>
      <div class="row">
        <div class="col">
          <%= render 'books/new', book: @book %>
        </div>
      </div>
    </div>
    <div class="col-lg-8 offset-md-1">
      <table class="table table-hover">
        <thead>
          <h2>Books</h2>
          <tr>
            <th scope="col"></th>
            <th scope="col">Title</th>
            <th scope="col">Opinion</th>
          </tr>
        </thead>
        <tbody>
        <% @books.each do |book| %>
          <tr>
            <td><%= image_tag book.user.get_profile_image(70,70) %></td>
            <td><%= book.title %></td>
            <td><%= book.body %></td>
          </tr>
        <% end %>
        </tbody>
      </table>
    </div>
  </div>
</div>